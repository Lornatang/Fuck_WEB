1、默认的SP.NET页面文件的扩展名是（.aspx）。

2、打开Visual Studio 2013的工具箱应使用系统的（视图）菜单。

3、ASP.NET采用C#，Visual Basic等语言为脚本，执行时一次编译，可以（多次）执行。

4、(C#)是.NET的标准语言。

5、ASP.NET程序代码在编译的时候NET框架先将源代码编译为（IL）。

6、以下不是动态网站的特性的是（数据稳定）。

7、对于页面中的HTML标记，可以直接嵌入数据或绑定表达式来设置要显示的数据，常用的绑定表达式采用（%#XXX%）形式。

8、ASP.NET页面的执行采用的是（B/S）架构。

9、下面（htm）是静态网页的扩展名。

10、小明在家里通过拨号上网访问新浪网站，此时服务器端和客户端的情况是（小明客户端，新浪服务器）。

11、NET是由（微软）公司推出的开发平台框架。

12、.NET为每种语言提供了一个编译器当网页第一次被访问时就会编译生成一种通用的（中间语言）。

13、.NET框架主要由3部分组成，分别是（公共语言运行时）、服务框架和应用程序模板。

14、.NET框架中的类分别放在了不同的（命名空间）中。

15、页面开头形如<%@…%>这样的代码称为（页面指令），是对本页面的某种声明。

16、ASP.NET的两种开发模式是Web Forms模式和（MVC）模式。注意大小写。

17、Visual Studio 2013提供了编辑设计、拆分和(源）3种视图。

18、ASP.NET的命名空间全部位于（System）下。注意大小写。

19、MVC模式
解：模型，视图，控制器
